# UNIWA Thesis Topics MCP Server

MCP server exposing `search_thesis_topics` for discovering diploma-thesis topics from official University of West Attica sources.

## Local run

Python 3.11+:

`pip install -r requirements.txt`

`python -m src.server`

Endpoint: `http://localhost:8000/mcp`

Deploy behind HTTPS for plugin use.
